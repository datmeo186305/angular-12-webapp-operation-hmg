import { FormatDateAndTimePipe } from './format-date-and-time.pipe';

describe('FormatDateAndTimePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDateAndTimePipe();
    expect(pipe).toBeTruthy();
  });
});
