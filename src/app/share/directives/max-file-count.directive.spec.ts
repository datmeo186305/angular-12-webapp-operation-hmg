import { MaxFileCountDirective } from './max-file-count.directive';

describe('MaxFileCountDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxFileCountDirective(null);
    expect(directive).toBeTruthy();
  });
});
