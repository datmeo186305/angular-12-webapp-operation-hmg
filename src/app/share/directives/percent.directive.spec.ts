import { PercentDirective } from './percent.directive';

describe('PercentDirective', () => {
  it('should create an instance', () => {
    const directive = new PercentDirective();
    expect(directive).toBeTruthy();
  });
});
