import { TypeMaxLengthDirective } from './type-max-length.directive';

describe('TypeMaxLengthDirective', () => {
  it('should create an instance', () => {
    const directive = new TypeMaxLengthDirective();
    expect(directive).toBeTruthy();
  });
});
