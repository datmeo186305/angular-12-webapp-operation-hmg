import { ImageFileOnlyDirective } from './image-file-only.directive';

describe('ImageFileOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageFileOnlyDirective(null);
    expect(directive).toBeTruthy();
  });
});
