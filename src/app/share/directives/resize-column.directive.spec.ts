import { ResizeColumnDirective } from './resize-column.directive';

describe('ResizeColumnDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizeColumnDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
